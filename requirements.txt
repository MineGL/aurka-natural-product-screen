# Pinned to the versions read from the executing environment (see SOFTWARE.md).
# Python 3.9.23. Covers the Python stages only; docking, interaction profiling and
# MD use standalone binaries documented in SOFTWARE.md.
#
# Superseded the unpinned list: these pins are measured, not assumed.
numpy==1.23.5
pandas==1.5.3
scipy==1.10.1
rdkit==2023.09.6
xgboost==2.1.4
scikit-learn==1.2.2
torch==2.1.2
dgl==1.1.2
deepchem==2.8.0
